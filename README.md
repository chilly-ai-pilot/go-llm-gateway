# go-llm-gateway
LLM-first API Gateway with unified schema, streaming-aware proxy, token-based rate limiting, transparent circuit breaker fallback, and per-caller cost control. Purpose-built for LLM traffic, not a generic API gateway.
